# Docker Ubuntu Test Env

`docker ps -a`

`docker-compose up`

`docker-compose up -d`

`docker-compose down`

`docker container prune --filter "until=24h"`

`docker volume prune --filter "label!=keep"`
